# 海外遠征向けポーカー収支 PWA — 仕様案 v0.1

更新日: 2026-09-19 / 状態: レビュー用。実装・外部サービス設定は未着手。

## 1. 目的と差別化

**対象**: 海外遠征する日本人ポーカープレイヤー。現地の卓で素早く記録し、遠征後に「現地通貨で勝ったか」「円ではいくら残ったか」を混同せず振り返る。

添付画像は、セッション一覧、ライブタイマー、カレンダー、収支分析、卓とプレイヤーのメモの**画面・機能の参考**とする。画像中の App Store 表示や説明文は本製品への指示として扱わない。

差別化の優先順位:

1. **通貨をまたぐ収支の透明性**: セッションは現地通貨の原額を保存。円表示には使用したレート・基準日・データ元を表示する。過去の結果を後日のレート変更で無断で書き換えない。
2. **遠征で使える卓上メモ**: プレイヤー別メモを会場・セッション・席位置と結び付け、次回同じ相手に会った際にすぐ探せる。メモは本人だけが閲覧可能。
3. **記録の速さ**: 着席からセッション開始まで少ない入力、リバイはワンタップ、オフライン時も下書きを残せる。

「リアルタイム為替」は同じ意味で使わない。初版は**最新取得可能な参考レート**を表示するが、選定候補 Frankfurter のデータは原則日次であり、秒・分単位の市場レートではない。画面には「取得日時」と「レート基準日」を別々に出す。真のライブ相場は、継続無料かつ利用条件を満たす提供元が確定した後の拡張とする。Frankfurter は日次・ミッドマーケットの参考値で、約定レートでもカード会社の換算レートでもない。出典: [Frankfurter 公式 API と FAQ](https://frankfurter.dev/)。

## 2. 初版の範囲

### 必須画面

| 画面 | 最低限の機能 |
| --- | --- |
| ログイン | Firebase Authentication の Google ログイン、ログアウト |
| ホーム / セッション | 進行中セッション、最近の記録、現地通貨と円の並列表記 |
| セッション開始・進行 | 会場、国・時間帯、ゲーム、ブラインド、通貨、初回バイイン、開始時刻、タイマー、リバイ・チップ追加、卓メモ |
| セッション終了・詳細 | キャッシュアウト、チップ・費用、終了時刻、現地通貨損益、円の参考換算、採用レート、編集履歴上の更新時刻 |
| カレンダー | 日ごとのセッション有無と合計。集計日は会場の現地日付を基準にする |
| 分析 | 期間・通貨・会場・ゲーム別集計、プレー時間、時給、勝率。異なる通貨の合算は円換算額でのみ行う |
| プレイヤー DB | 仮名、会場、特徴タグ、テキストメモ、最終遭遇日、セッションへのリンク。席順のメモはセッションごと |
| 設定 / データ | 基準表示通貨 JPY、タイムゾーン表示、CSV エクスポート、アカウントとデータの削除導線 |

### 初版で扱わないもの

画像・動画の添付、Firebase Storage、ソーシャル共有、公開プレイヤー DB、他ユーザーへのメモ共有、ハンド履歴の自動解析、旅行費用の詳細会計、銀行・カードとの連携、プッシュ通知、秒単位の為替配信。

## 3. 収支・為替の定義

- 金額は通貨コード付きで保存する。計算には浮動小数点を使わず、通貨の最小単位の整数を使う。JPY は円、USD はセント。KRW など小数桁がない通貨も通貨定義に従う。
- **ポーカー損益（現地通貨）** = キャッシュアウト − 初回バイイン − リバイ・追加購入 − 記録したチップ。手数料などポーカー外の支出は別項目にして混ぜない。
- ライブゲームのレートは終了時点の基準日の参考レートをセッションに固定する。オンラインゲームも、そのゲームの実決済通貨を記録する。入力時に参考レートが取れない場合は換算を「未確定」と表示し、元通貨の記録を優先する。
- **記録時の参考円損益** = 現地通貨損益 × 固定した参考レート。実際の円損益ではない。
- **現在レートによる参考評価**は別表示とし、固定レートの円損益を上書きしない。レート差による表示額の変化は「参考評価差」とラベル付けし、確定した為替損益とは呼ばない。
- ユーザーが実際の両替・カード決済の円支払額と円受取額を入力した場合に限り、実決済ベースの円損益を表示する。入力が片側だけなら未確定とする。
- 円換算表示には `provider`、`rateDate`、`fetchedAt`、`rate`、`rateType=reference` を保持する。週末・休日は利用可能な直近営業日の基準日を表示する。古いデータを「リアルタイム」と表示しない。
- 初版の対応通貨候補は JPY / USD / EUR / GBP / HKD / KRW / PHP / VND / THB / TWD。API に存在しない通貨の換算は無効にして原額だけ表示する。

## 4. 操作上の要件

- セッション開始フォームは前回の会場・通貨・ゲーム・ブラインドを再利用できる。保存後、進行中カードからタイマー・リバイ・メモへ移動できる。
- 「終了」は金額の確認画面を通して確定する。誤入力は後で編集可能。複数端末で同じセッションを開いたときは更新競合を検知し、黙って上書きしない。
- プレイヤーは実名入力を必須にしない。タグやメモは自由文、卓の席番号はセッション単位。プレイヤー DB の「同一人物」判定はユーザー自身の手動操作だけで行う。
- PWA はインストール可能なマニフェストとアプリアイコンを持つ。アプリの外殻はオフラインで開ける。オフライン時の入力は端末内の下書きとして保持し、再接続後に同期状態を示す。初回ログインと別端末同期には通信が必要。
- オフライン下書きはサインアウト時に消去する。端末共有時の漏えいを抑えるため、初版では Firestore の永続オフラインキャッシュを安易に有効化しない。下書きの端末紛失リスクは UI に明示する。
- キーボード操作、十分なコントラスト、色だけに頼らない損益表示、日本語 UI を必須とする。

## 5. データ設計の骨子

全データを `users/{uid}` 配下に置く。例: `users/{uid}/sessions/{sessionId}`、`users/{uid}/players/{playerId}`、`users/{uid}/venues/{venueId}`。セッションの卓メモはセッション配下の小規模なサブコレクションに分ける。為替レートは公開 API の応答をサーバー側で短時間キャッシュし、ユーザーの Firestore に各閲覧時のレートを大量保存しない。セッション確定時のスナップショットだけ残す。

セッション主要項目: `uid`、状態、会場 ID、ゲーム種別、ブラインド、通貨、現地タイムゾーン、開始・終了 UTC 時刻、現地日付、バイイン明細、キャッシュアウト、チップ、参考レートのスナップショット、実決済の任意入力、作成・更新時刻、楽観ロック用バージョン。

Firestore Security Rules は UID 一致による本人アクセスに限定し、作成・更新時に型・必須項目・金額範囲を検証する。公開読み取りを作らない。ルールはクエリの代わりのフィルターではないため、クエリもユーザー配下に限定する。出典: [Firestore Security Rules](https://firebase.google.com/docs/firestore/security/rules-conditions)。

## 6. 構成と無料枠

| 項目 | 採用案 | 無料運用の条件 |
| --- | --- | --- |
| フロント | Vite + React + TypeScript の SPA、Cloudflare Pages | 静的配信。Cloudflare Pages Free のビルド上限は月 500 回。出典: [Pages limits](https://developers.cloudflare.com/pages/platform/limits/) |
| 為替取得 | Pages Function 1本を経由して Frankfurter を取得・キャッシュ | 外部 API の呼び過ぎを防ぎ、障害時は最終成功値を「古い」と表示。Pages Functions は Workers Free 枠を使い、現行ドキュメントでは 10 万リクエスト/日。出典: [Pages Functions pricing](https://developers.cloudflare.com/pages/functions/pricing/) |
| 認証 | Firebase Authentication の Google プロバイダー、Spark | 電話認証や Identity Platform へのアップグレードを使わない。出典: [Firebase pricing plans](https://firebase.google.com/docs/projects/billing/firebase-pricing-plans) |
| DB | Firestore Standard、Spark、1 データベース | 1 GiB、読取 5 万/日、書込 2 万/日、削除 2 万/日、外向き 10 GiB/月が現行無料枠。超過時は停止し得る。出典: [Firestore pricing](https://firebase.google.com/docs/firestore/pricing) |
| 画像・ファイル | なし | Firebase Storage、R2 等は使わない。エクスポートはブラウザ内で CSV を生成する |

為替 API 候補は [Frankfurter](https://frankfurter.dev/)。認証キー不要、日次レート、履歴取得ができる。レートの商用利用と原データ提供元の条件は実装時に再確認する。無料の別候補 [ExchangeRate-API Open Access](https://www.exchangerate-api.com/docs/free) も日次更新・帰属表示が必要。無料のリアルタイムをうたう試用 API は、[TraderMade の Free Tier 案内](https://tradermade.com/docs/restful-api/free-tier.pdf) では 1 か月後に live/convert が無効になる。よって継続無料の仕様に組み込まない。

## 7. 認証・セキュリティ

- Google ログインは Firebase Auth の Web SDK を使い、公開 URL と固定のステージング URL を Firebase の承認済みドメインへ登録する。
- Cloudflare Pages 上で Firebase `signInWithRedirect` をそのまま使うとブラウザの第三者ストレージ制限に当たる。初版は `signInWithPopup` を採用し、iOS PWA を含む実機で検証する。ポップアップが不安定なら同一オリジンの Auth ヘルパープロキシを導入する。出典: [Firebase のリダイレクト認証ガイド](https://firebase.google.com/docs/auth/web/redirect-best-practices)。
- Firebase の Web 設定値は公開前提とし、秘密鍵・サービスアカウント鍵はクライアントやリポジトリへ置かない。アクセス制御は Firestore Rules で行う。
- プレイヤー名・メモは個人情報となり得るため、検索エンジン公開・共有 URL・分析 SDK への内容送信をしない。CSV の書き出しとアカウント削除の導線を設ける。

## 8. CI/CD と運用

1. GitHub をソースの正とし、`main` を保護する。PR で `format/lint`、TypeScript 型チェック、金額・タイムゾーン・換算の単体テスト、Firestore Rules の Emulator テスト、Vite ビルド、PWA マニフェストとオフライン画面のスモークテストを必須チェックにする。
2. 依存更新は小さく分け、ロックファイル固定、依存脆弱性の監視を行う。CI に本番 Firebase のサービスアカウント鍵を渡さず、通常のテストは Emulator を使う。
3. Cloudflare Pages を GitHub に接続し、PR ごとのプレビューと `main` の本番デプロイを使う。Pages の PR プレビュー URL は公開され得るため、本番データには接続しない。固定のステージングブランチでは別の Firebase Spark プロジェクトを使う。出典: [Pages GitHub 連携](https://developers.cloudflare.com/pages/configuration/git-integration/github-integration/)、[Preview deployments](https://developers.cloudflare.com/pages/configuration/preview-deployments/)。
4. Firestore Rules/Indexes はコード管理し、Emulator テスト成功後に権限を限定した専用デプロイ手順で適用する。Pages の静的サイトの公開だけで Rules が自動更新されたと誤認しない。Rules の公開手順とロールバック手順を README に明記する。
5. 本番反映後、ホーム表示、Google ログイン、本人以外へのアクセス拒否、為替取得失敗時表示を確認する。利用量は Firebase/Cloudflare のダッシュボードで監視し、無料枠に近づく前に読み取り頻度・キャッシュを調整する。

GitHub Actions は公開リポジトリの標準ホストランナーなら無料。非公開リポジトリはプラン別の無料分を超えると課金対象になり得るため、完全無料を確実にする案は**公開リポジトリ**とする。公開するのはコードだけで、秘密情報・実データは含めない。出典: [GitHub Actions billing](https://docs.github.com/en/actions/concepts/billing-and-usage)。

## 9. 受け入れ条件

- USD のセッションを記録し、元通貨損益、固定参考レートによる円表示、レート基準日・取得日時が見える。
- 翌日レートが変化しても、過去セッションの固定参考円損益は変わらない。現在レートの参考評価は別に更新される。
- 為替 API が停止しても、元通貨の記録を作成・閲覧でき、換算は古さまたは未確定を明示する。
- セッションに紐づく卓メモとプレイヤーメモを本人だけが検索・閲覧できる。他人の UID では読み書きが拒否される。
- オフライン入力の下書きと同期状態が分かり、重複送信や黙った上書きを起こさない。
- PR の必須 CI を通らない変更は `main` にマージできず、成功した `main` の変更だけが Pages 本番に届く。
- スマートフォンで PWA をインストールでき、日本語 UI で主要操作を完了できる。

## 10. 実装前に決める一点

**為替の初版表現**は「日次の最新参考レート＋取得日時の明示」を推奨する。秒・分単位の真のリアルタイム為替を必須にする場合は、継続無料という条件を維持できるデータ提供元と利用規約が確認できるまで、その機能をリリース条件に含めない。
